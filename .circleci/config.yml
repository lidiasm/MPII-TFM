version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7
      - image: circleci/mongo:latest
      - image: circleci/postgres:latest
        environment:
          POSTGRES_USER: $POSTGRES_USER
          POSTGRES_PASSWORD: $POSTGRES_PWD
    steps:
      - checkout
      - run:
          name: Install dependencies.
          command: |
            make install
            sudo apt-get install postgresql-client
      - run:
          name: Create database.
          command: |
            psql -h localhost -p 5432 -c "CREATE USER ${POSTGRES_USER} WITH PASSWORD '${POSTGRES_PSWD}';"
            psql -h localhost -p 5432 -c "CREATE DATABASE socialnetworksdb WITH OWNER=${POSTGRES_USER};"
            psql -h localhost -p 5432 --dbname=socialnetworksdb --file=postgresql_tables.sql
      - run:
          name: Run tests.
          command: make test
machine:
  services:
    - mongodb
    - postgresql